# Errata

Publication-facing errata are mirrored at the repository root in `errata.yml`
and under `catalog/corpus-governance/`.

Corpus owns the canonical semantic errata source. This repository mirrors the
public-safe snapshot so artifact folders, checksums, and citation guidance can
be reviewed together without changing PDF bytes.
